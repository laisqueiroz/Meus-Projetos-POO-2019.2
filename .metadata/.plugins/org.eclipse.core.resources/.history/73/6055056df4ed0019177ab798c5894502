package agencia;

public class ContaCorrente extends Conta {

    public ContaCorrente(String nome) {
        this.setId(nextId);
        this.setNome(nome);
        this.setSaldo(getSaldo());
        this.setTipo("CC");
        nextId++;
    }
}